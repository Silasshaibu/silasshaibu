/*At tablet size query all selector and set their grid column to be same as their grid row*/

// var screenWidth = window.innerWidth;

// if (screenWidth <= 600){
//     var divElements = document.querySelectorAll('.item');

// divElements.forEach(function(element) {
//   element.classList.remove('item--1x1', 'item--1x2', 'item--2x1', 'item--2x2');
// });
// }

